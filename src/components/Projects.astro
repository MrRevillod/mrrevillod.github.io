---
import pagesMap from "../utils/pages";
import PageCard from "./PageCard.astro";
---

<article class="w-full flex flex-col justify-center gap-10">
  <section class="flex flex-col justify-between gap-4">
    <h2 class="text-xl font-bold">Proyectos y enlaces de interés</h2>
    <p>
      En mi experiencia los proyectos son un método esencial de aprendizaje, con
      ellos te enfrentas a problemas reales a los cuales debes dar una solución
      óptima y tecnológica.
    </p>
  </section>

  <section
    class="grid grid-cols-1 md:grid-cols-2 gap-6"
  >
    {
      pagesMap.map(({ pageName, imgSrc, url, desc }) => (
        <PageCard imgSrc={imgSrc} url={url} desc={desc} pageName={pageName} />
      ))
    }
  </section>
</article>
